/**
 * @format
 */
'use strict';

import {AppRegistry} from 'react-native';
import App from 'middle/src/App';
import {name as appName} from 'middle/app.json';

AppRegistry.registerComponent(appName, () => App);
